import { useEffect, useRef } from "react";
export function CustomCursor(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!matchMedia("(pointer:fine)").matches)return;const el=ref.current;if(!el)return;const move=(e:PointerEvent)=>{el.style.transform=`translate3d(${e.clientX}px,${e.clientY}px,0)`};const over=(e:PointerEvent)=>{el.classList.toggle("active",!!(e.target as Element)?.closest("a,button"))};addEventListener("pointermove",move);addEventListener("pointerover",over);return()=>{removeEventListener("pointermove",move);removeEventListener("pointerover",over)}},[]);return <div ref={ref} className="custom-cursor"/>}
