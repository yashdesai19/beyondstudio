import { useEffect, useRef } from "react";
export function CustomCursor(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!matchMedia("(pointer:fine)").matches)return;const el=ref.current;if(!el)return;const move=(e:PointerEvent)=>{el.style.transform=`translate3d(${e.clientX}px,${e.clientY}px,0)`};const over=(e:PointerEvent)=>{const target=(e.target as Element)?.closest("a,button");const project=(e.target as Element)?.closest(".project-card");el.classList.toggle("active",!!target);el.classList.toggle("project",!!project);el.textContent=project?"VIEW":""};addEventListener("pointermove",move);addEventListener("pointerover",over);return()=>{removeEventListener("pointermove",move);removeEventListener("pointerover",over)}},[]);return <div ref={ref} className="custom-cursor" aria-hidden="true"/>}
